import { theme } from '@styles/default.theme';
import { View } from 'react-native';
import styled from 'styled-components';

export const View_Back = styled(View)`
  flex: 1;
  background: ${ theme.colors.primary.BgTela };
`;
